<!-- Merge these entries inside the generated Capacitor <application> element. -->
<receiver
    android:name="dev.yuss.homi.widget.HomiWidgetProvider"
    android:exported="false">
    <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
    </intent-filter>
    <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/homi_widget_info" />
</receiver>

<activity
    android:name="dev.yuss.homi.widget.HomiWidgetConfigurationActivity"
    android:exported="false" />
