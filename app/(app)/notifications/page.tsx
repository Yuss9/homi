import { NotificationWorkspace } from "@/src/components/notification-workspace";

export const metadata = { title: "Notifications" };
export default function Page() {
  return <NotificationWorkspace />;
}
