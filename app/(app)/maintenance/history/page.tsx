import { MaintenanceHistoryWorkspace } from "@/src/components/maintenance-history-workspace";

export const metadata = { title: "Maintenance history" };

export default function Page() {
  return <MaintenanceHistoryWorkspace />;
}
