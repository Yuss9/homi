import { MaintenanceTemplateWorkspace } from "@/src/components/maintenance-template-workspace";

export const metadata = { title: "Maintenance library" };

export default function Page() {
  return <MaintenanceTemplateWorkspace />;
}
